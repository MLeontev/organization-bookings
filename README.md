# organization-bookings

Frontend application for organization bookings.

## Local run

```bash
npm install
npm run dev
```

## Docker

```bash
docker compose up --build
```

The app is served by Nginx on `http://localhost:3000`.
It is attached to the external Docker network `booking-network`.
